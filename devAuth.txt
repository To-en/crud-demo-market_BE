# Run to generate JWT secret for your development

openssl rand -base64 64

# Two ways for dev to bypass auth for testing

## 1. set auth key สำหรับ dev โดยเฉพาะ จากนั้นให้ไปเขียนใน Auth code ของ project ให้ accept text ตัวนี้
DEV_TOKEN = supersecret-dev-only-abc123

## 2. Run to generate refresh token of admin access 
node -e "
import Jwt from 'jsonwebtoken';
console.log(Jwt.sign({ id: 1, role: 2 }, '7g/2RFlaizjRU+PdQIHnTDiAyb0IhOOHjBE+kvK0fPk=', { expiresIn: '365d' }))
"
